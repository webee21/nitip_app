import 'package:flutter/material.dart';

class snack extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Snack'),
      ),
      body: Center(
        child: Text('Halaman Detail Restaurant'),
      ),
    );
  }
}